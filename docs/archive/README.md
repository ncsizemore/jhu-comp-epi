# Archived Working Notes

This directory keeps historical implementation notes from previous site
iterations. They are retained for context, but the current setup and workflow
should be documented in the repository root `README.md` and focused docs under
`docs/`.
